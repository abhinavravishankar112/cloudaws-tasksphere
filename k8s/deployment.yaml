apiVersion: apps/v1
kind: Deployment
metadata:
  name: tasksphere-api
  namespace: tasksphere
spec:
  replicas: 2
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: tasksphere-api
  template:
    metadata:
      labels:
        app: tasksphere-api
    spec:
      containers:
        - name: api
          image: ghcr.io/your-org/cloudaws-tasksphere:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: tasksphere-config
            - secretRef:
                name: tasksphere-secret
          readinessProbe:
            httpGet:
              path: /api/v1/health/ready
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /api/v1/health/live
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
