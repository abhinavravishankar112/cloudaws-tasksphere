apiVersion: v1
kind: Service
metadata:
  name: tasksphere-api
  namespace: tasksphere
spec:
  selector:
    app: tasksphere-api
  ports:
    - name: http
      port: 80
      targetPort: 3000
  type: ClusterIP
